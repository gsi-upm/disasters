<!--
  <h3>Agente de ciudadano</h3>
-->
<agent xmlns="http://jadex.sourceforge.net/jadex" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xsi:schemaLocation="http://jadex.sourceforge.net/jadex http://jadex.sourceforge.net/jadex-bdi-2.0.xsd"
		name="ciudadano" package="disasters.caronte.simulador.usuarios">
	<imports>
		<import>disasters.Position</import>
		<import>disasters.caronte.Entorno</import>
		<import>jadex.base.fipa.IDF</import>
		<import>jadex.base.fipa.SFipa</import>
		<import>jadex.bdi.planlib.protocols.request.Request</import>
		<import>jadex.bdi.planlib.df.DF</import>
		<import>jadex.bridge.service.RequiredServiceInfo</import>
		<import>jadex.bridge.service.SServiceProvider</import>
		<import>jadex.commons.future.IFuture</import>
	</imports>

	<capabilities>
		<capability name="procap" file="Request"/>
		<capability name="dfcap" file="DF"/>
	</capabilities>

	<beliefs>
		<!-- Creamos el agente como instancia del Entorno. Por defecto, en la residencia -->
		<belief name="env" class="Entorno">
			<fact>Entorno.getInstance(Entorno.CIUDADANO, $scope.getAgentName(), $beliefbase.residencia)</fact>
		</belief>
		<!-- Definimos la posicion de la residencia -->
		<belief name="residencia" class="Position">
			<fact>new Position(38.232272, -1.698925)</fact>
		</belief>
		<!-- Posicion del agente -->
		<belief name="pos" class="Position">
			<fact>$beliefbase.env.getAgent($scope.getAgentName()).getPosition()</fact>
		</belief>
		<!-- ID del desastre que atiende -->
		<belief name="idEmergencia" class="Integer">
			<fact>0</fact>
		</belief>
	</beliefs>

	<goals>
		<achievegoalref name="rp_initiate">
			<concrete ref="procap.rp_initiate"/>
		</achievegoalref>
		<achievegoalref name="df_search">
			<concrete ref="dfcap.df_search"/>
		</achievegoalref>
		<maintaingoalref name="df_register">
			<concrete ref="dfcap.df_register"/>
		</maintaingoalref>

		<achievegoal name="registro"/>
	</goals>

	<plans>
		<plan name="registro-plan">
			<body class="RegistroPlan"/>
			<trigger>
				<goal ref="registro"/>
			</trigger>
		</plan>
	</plans>

	<properties>
		<property name="dfservice" class="IFuture">SServiceProvider.getService($scope.getServiceProvider(), IDF.class, RequiredServiceInfo.SCOPE_PLATFORM)</property>
	</properties>

	<configurations>
		<configuration name="default">
			<goals>
				<initialgoal ref="df_register">
					<parameter ref="description">
						<value>
							$properties.dfservice.createDFComponentDescription(null,
								$properties.dfservice.createDFServiceDescription("ciudadano-service", "ciudadano", "GSI DIT UPM"))
						</value>
					</parameter>
				</initialgoal>
				<initialgoal ref="registro"/>
			</goals>
		</configuration>
	</configurations>
</agent>